#In Python, input() is a built-in function that allows a program to receive user input. 
#It reads a line from the user, converts it to a string, and returns that string.

naamBato = input("apna naam likh le bhai : ")
phoneNo = int(input("muje number bhi chaiye : "))
pointWala = float(input("ab muje koi point wala number do : "))

 